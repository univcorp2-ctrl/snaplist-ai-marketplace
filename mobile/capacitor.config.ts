import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.snaplistai',
  appName: 'SnapList AI',
  webDir: '../web',
  server: { androidScheme: 'https' }
};

export default config;
